# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Drop full-res photography originals in src/assets/photography. They stay
# local; only the generated src/assets/photography-optimized WebPs are committed.
src/assets/photography/*
!src/assets/photography/.gitkeep
*.local

# Local secrets. The Twitch client secret lives here in dev and in the Netlify
# UI in production — it must never be committed.
.env
.env.*
!.env.example

# Netlify CLI state
.netlify

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
